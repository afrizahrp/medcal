Audit ONLY. DO NOT implement, modify, refactor, create, delete, migrate, rename, or otherwise change any file, database schema, source code, configuration, route, UI, API, Prisma schema, or documentation. DO NOT make any code changes even if you find issues. Your job in this task is strictly to inspect the existing repository, existing implementation, existing documentation, and existing plans, then produce a comprehensive audit report of the implementation plan for the Technician App and Portal Management App.

Audit the end-to-end business transaction lifecycle for the Medcal/Kalibrasi Medika system, covering the flow from Calibration Request → Quotation → Purchase Order → Work Order → Invoice → Payment. The audit must evaluate whether the planned architecture and implementation are internally consistent, complete, correctly sequenced, and compatible with the existing customer and document-numbering architecture.

The two application areas being audited are the Technician App and the Portal Management App, with the management portal intended to operate under apps.kalibrasimedika.co.id. Treat this as an architectural and implementation-readiness audit, not as a coding task.

You MUST inspect the repository and relevant existing implementation before drawing conclusions. Do not assume that a feature exists merely because it is mentioned in a plan. Distinguish clearly between: already implemented, partially implemented, planned/documented only, referenced but not implemented, and missing entirely.

The technician-specific source documentation is located at D:\medcal\docs\technician-docs. Read and inspect the relevant documents there comprehensively. The customer audit documentation is located at D:\medcal\docs\cursor\plan\customer.md. The purchase-order audit documentation is located at D:\medcal\docs\cursor\plan\purchase-order.md. These documents are mandatory audit inputs and must be cross-checked against the actual repository implementation rather than treated as authoritative by default.

A critical architectural constraint is that document numbering is already implemented at the Customer creation stage through the existing documentNumberingSequence mechanism. Audit how this existing numbering architecture is intended to propagate or be reused throughout subsequent transaction documents. Specifically verify the numbering requirements and consistency for Calibration Request, Quotation, Purchase Order, Work Order, Invoice, and Payment. Determine whether each document type has an appropriate numbering sequence, whether numbering is generated at the correct lifecycle point, whether numbering is tenant/company/entity aware where required, whether uniqueness is guaranteed, whether concurrency is handled safely, whether numbering can be consumed prematurely, whether failed/aborted transactions create gaps that are acceptable by design, and whether subsequent documents correctly preserve their relationship to the originating transaction. Do not propose a completely separate numbering mechanism unless the existing architecture demonstrably cannot support the required lifecycle.

Audit the business relationships between all documents. Verify the expected cardinality and lifecycle relationships between Customer, Calibration Request, Quotation, Purchase Order, Work Order, Invoice, Payment, and the underlying calibration items/equipment. Pay particular attention to whether one transaction may contain multiple calibration items, whether individual items can progress independently, whether partial completion is supported, whether partial invoicing is supported, whether payment can be allocated correctly, and whether the lifecycle remains consistent when only some items within a transaction have completed their required work. Identify any contradiction between the planned application behavior and the existing domain model.

Audit the Technician App specifically for the actual operational workflow of technicians. Determine what information technicians receive, what information they are allowed to modify, which workflow states they can transition, what constitutes starting work, completing work, recording calibration results, attaching evidence/documents/photos where applicable, handling failed or rejected work, handling partially completed work, and how technician actions affect the Work Order and downstream Invoice/payment eligibility. Verify whether technician permissions are appropriately separated from management/customer/administrative permissions. Check whether the technician app depends on APIs, entities, statuses, or workflow transitions that are not currently defined or implemented.

Audit the Portal Management App specifically for the management-side workflow. Verify how management users create, review, approve, reject, revise, and progress Calibration Requests, Quotations, Purchase Orders, Work Orders, Invoices, and Payments. Verify whether the UI/workflow expectations match the backend/domain model. Check whether every important state transition has a clear owner, permission requirement, validation rule, audit trail, and downstream consequence.

Audit the complete state machine across the transaction lifecycle. Identify every relevant status/state and verify that transitions are explicit and logically valid. Look for impossible transitions, missing transitions, ambiguous statuses, duplicated concepts represented by different fields, status fields that can become contradictory, transitions that can be performed by unauthorized roles, and transitions that do not correctly propagate to dependent documents. Pay special attention to cancellation, rejection, revision, reopening, partial completion, partial invoicing, payment allocation, and document reversal scenarios.

Audit API boundaries and backend responsibilities. Trace the relevant routes/controllers/services/use-cases/repositories and determine whether business rules are enforced server-side rather than only in the UI. Verify whether critical transitions are transactional and atomic where necessary. Identify race-condition risks, duplicate creation risks, duplicate numbering risks, stale-state risks, and idempotency problems. Pay particular attention to actions that may be triggered twice by browser retries, mobile networks, double-clicks, reconnects, or repeated API requests.

Audit database/domain-model consistency. Inspect Prisma schemas, migrations, relations, indexes, unique constraints, foreign keys, nullable fields, enums, status fields, timestamps, soft-delete behavior if present, and any audit/history tables relevant to the lifecycle. Determine whether the current schema can actually support the planned Technician App and Portal Management App without introducing hidden inconsistencies. Identify fields that are required by the workflow but absent from the model, and fields that appear redundant or semantically ambiguous.

Audit authorization and role boundaries. Verify the roles and permissions involved in customer management, management portal operations, technician operations, quotation/PO approval, Work Order execution, invoice generation, payment recording, and document visibility. Determine whether authorization is enforced at the API/service layer and whether object-level access control prevents one customer, technician, organization, or tenant from accessing another party’s data. Do not assume authentication alone is sufficient.

Audit multi-tenant/company/entity isolation wherever applicable. Verify that Customer, transaction documents, numbering sequences, Work Orders, Invoices, Payments, technicians, and related records are consistently scoped to the correct organization/company/entity. Look for queries that could accidentally return cross-tenant data or numbering collisions.

Audit the document lifecycle and traceability. Every downstream document should be traceable back to the correct upstream business context. Verify that the system can answer questions such as: which Customer owns this request, which Quotation originated this PO, which PO generated this Work Order, which Work Order items generated this Invoice, which Invoice is being paid, and which Payment allocations settle which invoice amounts. Identify any point where the chain becomes ambiguous or is only maintained through informal conventions rather than explicit relations.

Audit partial-processing behavior in depth. Do not assume that a Work Order, Invoice, or Payment is always all-or-nothing. Determine whether individual calibration items/equipment can move through the lifecycle independently. Verify whether completed items can be invoiced without waiting for all items in the same originating transaction to complete. Verify whether invoice totals, outstanding amounts, payment allocations, and document statuses remain mathematically and logically correct under partial processing.

Audit revision and immutability rules. Determine which documents become immutable after approval or downstream consumption, which fields may still be changed, and what should happen if an upstream document must be revised after a downstream document already exists. Identify whether the existing plan prevents dangerous mutations such as changing a Customer, PO item, price, quantity, or Work Order after downstream documents have relied on those values.

Audit financial consistency around quotation, PO, invoice, and payment. Verify source-of-truth ownership for prices, quantities, taxes, discounts, totals, invoiceable amounts, paid amounts, outstanding amounts, and payment allocations. Identify whether calculations are duplicated across frontend/backend or whether there is a reliable authoritative calculation path. Look for rounding, decimal precision, currency, tax, discount, and aggregation risks.

Audit auditability. Verify whether critical business actions have sufficient history to determine who performed them, when they occurred, what changed, and why. This is especially important for approval/rejection, Work Order execution, completion, invoice creation, payment recording, cancellation, and revisions.

Audit frontend/backend contract consistency for both Technician App and Portal Management App. Check DTOs, API responses, form models, validation schemas, enums, status labels, and frontend assumptions against the backend implementation. Identify places where frontend behavior assumes fields, statuses, endpoints, or relationships that do not actually exist.

Audit routing, application boundaries, and deployment assumptions relevant to apps.kalibrasimedika.co.id and the Technician App. Determine whether the current architecture clearly separates management portal concerns from technician-facing concerns while sharing the correct backend/domain services. Identify duplicated business logic between applications and determine whether that duplication creates consistency risks.

Audit the implementation sequence itself. Determine whether the proposed implementation order is safe. Specifically identify dependencies that must exist before Calibration Request, Quotation, PO, Work Order, Invoice, or Payment can be implemented correctly. Check whether the plan attempts to implement downstream workflows before the upstream domain contracts, statuses, numbering, permissions, and relations are stable. Identify circular dependencies or missing prerequisites.

Audit the existing Customer implementation and the documentNumberingSequence implementation as foundational infrastructure. Verify exactly how numbering is currently created, scoped, incremented, persisted, and consumed during Customer creation. Determine whether the same infrastructure is suitable for the other transaction documents. Do not infer its behavior from its name alone; inspect the actual implementation, database schema, migrations, service logic, transaction boundaries, and callers.

Cross-reference all findings against D:\medcal\docs\cursor\plan\customer.md and D:\medcal\docs\cursor\plan\purchase-order.md and the documentation under D:\medcal\docs\technician-docs. Explicitly identify contradictions between documentation and implementation. Also identify contradictions between different documents. If documentation says something is implemented but the repository does not support it, report that discrepancy. If the repository already implements something that the documentation does not account for, report that too.

Do not limit the audit to obvious TODOs. Search for hidden architectural risks, implicit assumptions, incomplete domain relationships, incorrect lifecycle modeling, duplicated business rules, missing constraints, race conditions, authorization gaps, and implementation-order problems.

Do not modify any file. Do not run commands that mutate the repository, database, generated files, lockfiles, migrations, dependencies, or configuration. Read-only inspection only. If a build, test, type-check, lint, or static analysis command is necessary for evidence, only use it if it is demonstrably read-only and does not generate or modify files. If a command would modify anything, do not run it; report that limitation instead.

The final audit must provide a clear overall verdict for the Technician App + Portal Management App implementation plan: GREEN, YELLOW, or RED. The verdict must be based on evidence from the repository and documentation, not optimism about the plan.

For every significant finding, identify the exact evidence: file path, relevant class/function/schema/route/component where applicable, and the specific reason it matters. Separate confirmed facts from assumptions and recommendations. Do not invent implementation details that you cannot verify.

At the end, provide a prioritized list of blockers that must be resolved before implementation can safely proceed, followed by high-risk issues, medium-risk issues, and low-risk issues. Also identify which parts of the planned implementation are already safe to proceed with and which parts should NOT be implemented yet because foundational decisions or dependencies are unresolved.

Again: THIS IS AN AUDIT ONLY. Do not implement fixes. Do not modify files. Do not create a migration. Do not refactor code. Do not silently correct anything while auditing. The repository must remain unchanged after this audit.